package com.example.UserAuthencationByEncryptedPassword;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class UserAuthencationByEncryptedPasswordApplication {

	public static void main(String[] args) {
		SpringApplication.run(UserAuthencationByEncryptedPasswordApplication.class, args);
	}

}
